by defining an error handling middleware function


checkParameter('/route', (req, res, next) => {
  // condition for check the things here
  // responce with solution or with error
  // then call - next()
});
